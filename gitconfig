[user]
    name = "Arpad Borsos"
    email = arpad.borsos@googlemail.com
    signingkey = 908EDF65263368B4

[color]
    ui = auto

[alias]
    st = status -s
    co = checkout
    ci = commit -v -a
    glog = log --graph --decorate
    psuh = push
    pum = pull upstream master
    pur = pull --rebase
    pun = push --no-verify
    rim = rebase -i master
    rc = rebase --continue

[core]
    editor = nvim
    pager = diff-so-fancy | less --tabs=4 -RFX

;[push]
;    default = current

[merge]
    tool = meld

[rerere]
    enabled = true

[credential]
    helper = store

[diff]
    compactionHeuristic = true
    renameLimit = 10000

[interactive]
    diffFilter = diff-so-fancy

[commit]
    gpgsign = true
[tag]
    gpgsign = true
[pull]
	rebase = true
